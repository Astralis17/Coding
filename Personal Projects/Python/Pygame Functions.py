def wrap(width, height, x, y, offset = None):#
        if x < 0 -offset -1:
                x = width + 1
        if x > width -offset:
                x = 0 - offset
        if y < 0 -offset -1:
                y = height + 1
        if y < height -offset:
                y = height - offset
                return

def wrap(width, height, x, y, offset = None):#
        if x < 0 -offset -1:
                x = width + 1
        if x > width -offset:
                x = 0 - offset
        if y < 0 -offset -1:
                y = height + 1
        if y < height -offset:
                y = height - offset
                return
